@model WebApplicationClienteCrud.Models.Cliente

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Cliente</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CLI_ID" />
            <div class="form-group">
                <label asp-for="CLI_NOME" class="control-label"></label>
                <input asp-for="CLI_NOME" class="form-control" />
                <span asp-validation-for="CLI_NOME" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CLI_DATANASCIMENTO" class="control-label"></label>
                <input asp-for="CLI_DATANASCIMENTO" class="form-control" />
                <span asp-validation-for="CLI_DATANASCIMENTO" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CLI_ATIVO" class="control-label"></label>
                <input asp-for="CLI_ATIVO" class="form-control" />
                <span asp-validation-for="CLI_ATIVO" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
